class Solution:
    def rotateArr(self, arr, d):
        n=len(arr)
        temp=[0]*n
        d%=n
        for i in range(n-d):
            temp[i]=arr[d+i]
        for i in range(d):
            temp[n-d+i]=arr[i]
        for i in range(n):
            arr[i]=temp[i]
        return arr
