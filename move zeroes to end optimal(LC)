class Solution:
    def moveZeroes(self, arr: List[int]) -> None:
        count=0
        n=len(arr)
        for i in range(n):
            if(arr[i]!=0):
                arr[count],arr[i]=arr[i],arr[count]
                count+=1
        return arr
